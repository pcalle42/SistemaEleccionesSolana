services:
  postgres:
    labels:
      votaciones.environment: devnet
    networks:
      - internal
      - migration-access
    ports:
      - 127.0.0.1:${POSTGRES_HOST_PORT:-55432}:5432

  valkey:
    labels:
      votaciones.environment: devnet

networks:
  migration-access:
    name: ${COMPOSE_PROJECT_NAME:-votaciones}_migration_access
