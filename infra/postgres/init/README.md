# Inicialización

Solo se permiten tareas infraestructurales que deban ejecutarse al crear el
cluster. `10-create-runtime-role.sh` prepara el rol runtime sin privilegios y la
base aislada de tests. El esquema de aplicación pertenece a migraciones
versionadas.
